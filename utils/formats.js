const DATE_FORMAT = 'DD/MM/YY';
const TIME_FORMAT = 'HH:mm';
const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const HOUR_REGEX = /^([01]\d|2[0-3]):[0-5]\d$/; // HH:mm

export { DATE_FORMAT, TIME_FORMAT, EMAIL_REGEX, HOUR_REGEX };
